// always use ===
// equal comparison doesn't work for non-primitive

const first = [65, 41, 65];
const second = [65, 41, 65];
if (first == second) {
    console.log('values are equal');
}
else {
    console.log('values are different');
}

// type coercion
// type conversion
// type casting